	/* ------------------------------------------------------------------------------------------------------------- */
	/* 						csp-invoices.txt						 */
	/* ------------------------------------------------------------------------------------------------------------- */
	/* This displays the list of invoices generated for transactions incurred for customer and operator accounts     */
	/* ------------------------------------------------------------------------------------------------------------- */

	include "csp-header.txt";
	include "csp-invoicelist.txt";

	HtmlHeader("Accords Platform Invoices",$1);

	$what = $2;

	$accounts = account.list( 'occi.account.name="'#$what#'"');
	$account = $accounts[0];

	"<tr><td colspan=6>".display();
	InvoiceList( 'occi.invoice.account="'#$account#'"' );
	"</td></tr>".display();

	$m = "<tr><th colspan=6><a href='/accords/dashboard.php?action=compile&output=HTML&script=csp-transactions.txt&option="#$what#"'>Process Transactions</a></th></tr>";
	$m.display();
	
	HtmlFooter();



