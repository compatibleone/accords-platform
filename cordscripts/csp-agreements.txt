	/* ------------------------------------------------------------------------------------------------------------- */
	/* 						csp-agreements.txt						 */
	/* ------------------------------------------------------------------------------------------------------------- */
	/* This script displays the list of agreements parsed into the system allowing access for inspection of each     */
	/* ------------------------------------------------------------------------------------------------------------- */

	include "csp-header.txt";
	include "csp-agreementlist.txt";

	HtmlHeader("Service Level Agreements",$1);

	$what = $2;

	$accounts = account.list( 'occi.account.name="'#$what#'"');
	$account = $accounts[0];

	"<tr><td colspan=6>".display();
	AgreementList( 'occi.agreement.initiator="'#$account#'"' );
	"</td></tr>".display();

	$m = "<tr><th colspan=6><a href='/accords/dashboard.php?action=compile&output=HTML&script=csp-newsla.txt&option="#$what#"'>Create a New Service Level Agreement</a></th></tr>"
	$m.display();
	
	HtmlFooter();



